package com.api.renault.repository;

import com.api.renault.models.NinaModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NinaRepository extends JpaRepository<NinaModel, Integer> {
}
